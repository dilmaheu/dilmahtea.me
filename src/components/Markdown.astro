---
import renderMarkdown from "@utils/renderMarkdown";

const [cid] = Object.keys(Astro.props),
  markdown = await Astro.slots.render("default"),
  html = renderMarkdown(markdown, cid);
---

<Fragment set:html={html} />
