---
declare interface Props {
  id: string;
  path: string;
}

const { id, path } = Astro.props as Props;
---

<svg height="0" width="0"
  ><defs
    ><clipPath id={id} clipPathUnits="objectBoundingBox"
      ><path d={path}></path>
    </clipPath>
  </defs>
</svg>
